---
navigation:
  title: 'DevOnly'
  icon: 'lucide:wrench'
---

# `DevOnly`

`DevOnly` is available from `vuloom/app`.

Use it for development-only UI, debug panels, and probes that should disappear from production renders.

It follows the same runtime behavior as the underlying `vuepagelet` component.
