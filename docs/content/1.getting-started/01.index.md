---
icon: lucide:info
---

# Introduction

Phial is a full-stack Vue framework built around a small set of conventions:

- `app/` owns your document shell, page routes, middleware, and app-level data.
- `server/routes/` owns raw HTTP handlers such as APIs, webhooks, and text responses.
- `phial.config.ts` owns project-level configuration.
- `phial prepare` generates `.phial/types/` so route ids, params, and middleware names stay typed.

Phial is designed for teams that want file-based routing and SSR without losing direct control over Vue, Vite, and request handling.

## What Phial includes

- Document-level hydration with `app/app.vue`.
- File-based page routing under `app/pages/`.
- Route `loader` and `action` files.
- Client navigation loading boundaries with `loading.*`.
- Raw server handlers with `server/routes/**/*.ts`.
- Typed navigation targets such as `{ routeId, params }`.
- App-level and route-level middleware with explicit ordering.

## The default workflow

1. Create a project.
2. Run `phial prepare` to generate local route and middleware types.
3. Run `phial dev` during development.
4. Run `phial build` for production output.
5. Run `phial start` to serve the built app.

## Project shape

```text
├── app/
│   ├── app.vue
│   ├── app.config.ts
│   ├── loader.ts
│   ├── error.vue
│   ├── middleware/
│   ├── pages/
├── server/
│   ├── middleware/
│   ├── routes/
├── public/
├── phial.config.ts
```

The rest of this section walks through that structure from first boot to first production build.
